version: '3.8'

services:
  juicios-evaluativos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sena-juicios-evaluativos
    restart: always
    ports:
      - "8000:80"
    environment:
      - NODE_ENV=production
